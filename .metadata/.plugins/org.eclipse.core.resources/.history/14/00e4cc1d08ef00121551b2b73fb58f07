#include <stdint.h>
#include <map>
#include <list>

// 8 characters used as identifier, passed as one 64bit int
typedef uint64_t Tag;
typedef uint64_t Time; // placeholder for now

Tag CharsToTag(char*);

class MainFrame;

class AudioStorage;
class Buffer;
uint32_t Buffer::GetSize();
uint32_t Buffer::SetSize(uint32_t size);
float Buffer::GetValue(uint32_t index);
void Buffer::SetValue(uint32_t index, float value);


class Drop;
class Engine;
class Idea;
class Interpreter;

class PassageList;
typedef PassageList* Passage;
class Puro;
class Worker;



